---
id: changing-token-lifetime
title: Changing the verification link's lifetime
hide_title: true
---

<!-- COPY DOCS -->
<!-- ./thirdpartyemailpassword/common-customizations/email-verification/changing-token-lifetime.mdx -->

import SelfHostedTabs from "/src/components/tabs/SelfHostedTabs"
import TabItem from '@theme/TabItem';

# Changing the verification link's lifetime

By default, the email verification link's lifetime is 1 day. This can be changed via a core's config (time in milliseconds):

<SelfHostedTabs>
<TabItem value="with-docker">

```bash
# Here we set the lifetime to 2 hours.

docker run \
    -p 3567:3567 \
    // highlight-next-line
    -e EMAIL_VERIFICATION_TOKEN_LIFETIME=7200000 \ 
    -d registry.supertokens.io/supertokens/supertokens-<db_name>
```
</TabItem>

<TabItem value="without-docker">

```yaml
# You need to add the following to the config.yaml file.
# The file path can be found by running the "supertokens --help" command

email_verification_token_lifetime: 7200000
```

</TabItem>
</SelfHostedTabs>

:::info
- For managed service, this values can be updated by visiting our dashboard.
- This requires that your SuperTokens core version >= `3.6.0`
:::
