import CustomAdmonition from "/src/components/customAdmonition"

<CustomAdmonition type="paid-feature">

This is a paid feature. [Sign up](/auth) to get a license key. This will start a **SuperTokens subscription**.

If you want to try this feature without contacting us, you can self host the SuperTokens core and run it with the in memory db, by not connecting it to a database when running it. All paid features are enabled for free when using the in memory db.

If you are using the development environment of our managed servicem you can use this feature for free.

</CustomAdmonition>
