---
id: securing-component
title: Securing a website route / component
hide_title: true
---

import FrontendSDKTabs from "/src/components/tabs/FrontendSDKTabs"
import TabItem from '@theme/TabItem';
import {Question, Answer}from "/src/components/question"

# Securing a website route / component
You may want to prevent rendering of some part of UI, or a route, when
there's no session.

To achieve that, wrap the component tree in `ThirdPartyEmailPasswordAuth`. When there's no session, this component
will redirect the user to the login page.

<FrontendSDKTabs>
<TabItem value="reactjs">
<Question
    question="Do you use react-router-dom?">
<Answer title="Yes">

```jsx 
// highlight-next-line 
import {ThirdPartyEmailPasswordAuth} from "supertokens-auth-react/recipe/thirdpartyemailpassword";

class App extends React.Components {
    render() {
        return (
            <Router>
                <Switch>
                    {...}
                    // highlight-start
                    <Route path="/dashboard">
                        <ThirdPartyEmailPasswordAuth>
                            // TODO: Components that require to be protected by authentication
                            <MyDashboardComponent />
                        </ThirdPartyEmailPasswordAuth>
                    </Route>
                    // highlight-end

                    // Other components without authentication
                    {...}
                </Switch>
            </Router>
        );
    }
}
```
</Answer>
<Answer title="No">

```jsx
// highlight-next-line 
import {ThirdPartyEmailPasswordAuth} from "supertokens-auth-react/recipe/thirdpartyemailpassword";

class Dashboard extends React.Components {
    render() {
       // highlight-start 
       return (
            <ThirdPartyEmailPasswordAuth>
                // TODO: Component UI
            </ThirdPartyEmailPasswordAuth>
        );
        // highlight-end
    }
}
```
</Answer>
</Question>
</TabItem>
</FrontendSDKTabs>

Read more about `ThirdPartyEmailPasswordAuth` [its API reference](/docs/auth-react/thirdpartyemailpassword/third-party-email-password-auth).
