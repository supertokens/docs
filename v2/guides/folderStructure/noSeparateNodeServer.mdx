import StackViewer from "/src/components/guides/StackViewer"
import Frontend from "/guides/folderStructure/frontend.mdx"
import BackendView from "/src/components/guides/BackendView"

## Folder structure overview

<StackViewer showIf={(selection) => {
    return selection.backend === "nextjs" && selection.frontend === "nextjs"
    }}>

Full stack NextJS folder structure in which we are using supertokens-node and supertokens-auth-react...

</StackViewer>

<StackViewer showIf={(selection) => {
    return selection.backend === "remix" && selection.frontend === "remix"
    }}>

Full stack Remix folder structure in which we are using supertokens-node and supertokens-auth-react...

</StackViewer>

<StackViewer showIf={(selection) => {
    return selection.backend === "nodejs" || selection.backend === "golang" || selection.backend === "python"
    }}>

#### `/backend`:

<BackendView backend="nodejs">

We have a nodejs backend in which we have added the supertokens-node SDK..

</BackendView>

<BackendView backend="golang">

We have a golang backend in which we have added the supertokens-golang SDK..

</BackendView>

<BackendView backend="python">

We have a python backend in which we have added the supertokens-python SDK..

</BackendView>

#### `/frontend`:

<Frontend />

</StackViewer>