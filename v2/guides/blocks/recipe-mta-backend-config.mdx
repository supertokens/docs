
In order to create a tenant, you need to define your own tenantId (a unique string/id for that tenant), and also define the login methods for that tenant.
You can then configure this in the **SuperTokens Core** via an API call or via the user management dashboard.
For more details on this step, checkout [this link](/docs/multitenancy/new-tenant)
