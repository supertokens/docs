---
hide: true
---

import { GuideCondition } from "/src/components/guides/GuideCondition"
import { GuideLabel } from "/src/components/guides/GuideLabel"

Use our starter project to get up and running quickly. It comes with full config examples 
based on your selection. Just run the following command to install everything:

<GuideCondition match={guide => guide.frontend === "nextjs" && guide.backend === "nextjs"}>

```bash
npx create-supertokens-app@latest --frontend=<GuideLabel value="frontendExampleAppInstallArgument" /> --recipe=<GuideLabel value="recipeExampleAppInstallArgument"
```

</GuideCondition>


<GuideCondition match={guide => guide.frontend !== "nextjs" || guide.backend !== "nextjs"}>

```bash

<pre>
<GuideLabel name="frontendTitle" />
</pre>
```

</GuideCondition>

