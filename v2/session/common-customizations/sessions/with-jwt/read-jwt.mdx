---
id: read-jwt
title: Getting the JWT
hide_title: true
---

<!-- COPY DOCS -->
<!-- ./session/common-customizations/sessions/with-jwt/read-jwt.mdx -->

import BackendSDKTabs from "/src/components/tabs/BackendSDKTabs"
import TabItem from '@theme/TabItem';
import BackendSDKCasing from "/src/components/BackendSDKCasing"
import FrontendSDKTabs from "/src/components/tabs/FrontendSDKTabs"
import FrontendReactContextSubTabs from "/src/components/tabs/FrontendReactContextSubTabs"
import NpmOrScriptTabs from "/src/components/tabs/NpmOrScriptTabs"

# Getting the JWT

:::caution
The examples on this page use the default key `jwt` for the access token payload, to know how to change the key visit [here](./enabling-jwts#using-a-custom-key-in-the-access-token-payload)
:::

### On the backend

In order to read the JWT, you first need to fetch session information. You can do this in the following ways:
- [Using verifySession on your APIs](../session-verification-in-api/verify-session)
- [Using session handles](../fetch-sessions-for-user)

<BackendSDKTabs>
<TabItem value="nodejs">

```jsx
// Get session information using one of the methods listed above
let sessionInformation = getSessionInformation();

let jwt = (sessionInformation.getAccessTokenPayload()).jwt;
```

</TabItem>
<TabItem value="go">

```go
WIP
```
</TabItem>
<TabItem value="python">

```python
WIP
```

</TabItem>
</BackendSDKTabs>

### On the frontend

<FrontendSDKTabs>
<TabItem value="reactjs">
<FrontendReactContextSubTabs>

<TabItem value="with-react-context">

#### Step 1: Wrap the component in which you want to get the info with `^{recipeNameCapitalLetters}Auth`

```jsx
import { ^{recipeNameCapitalLetters}Auth } from 'supertokens-auth-react/recipe/^{codeImportRecipeName}';

render() {
     return (
           <^{recipeNameCapitalLetters}Auth>
                  <Dashboard />
           </^{recipeNameCapitalLetters}Auth>
     );
}
```


#### Step 2: This is how to use the session context in a component:

```jsx

import React from "react";
// highlight-next-line
import { useSessionContext } from 'supertokens-auth-react/recipe/session'; 

// Your dashboard component
function Dashboard(props) {
    // highlight-next-line
    let {userId, accessTokenPayload} = useSessionContext();

    let jwt = accessTokenPayload.jwt;
}
```

</TabItem>

<TabItem value="without-react-context">

```jsx
import Session from 'supertokens-auth-react/recipe/session';

if (await Session.doesSessionExist()) {
      let userId = await Session.getUserId();
      let jwt = (await Session.getAccessTokenPayloadSecurely()).jwt;
}
```

</TabItem>
</FrontendReactContextSubTabs>
</TabItem>

<TabItem value="vanillajs">

<NpmOrScriptTabs>
<TabItem value="npm">

```jsx
import SuperTokens from 'supertokens-website';

if (await SuperTokens.doesSessionExist()) {
      let userId = await SuperTokens.getUserId();
      let jwt = (await SuperTokens.getAccessTokenPayloadSecurely()).jwt;
}
```

</TabItem>
<TabItem value="script">

```jsx
if (await supertokens.doesSessionExist()) {
      let userId = await SuperTokens.getUserId();
      let jwt = (await SuperTokens.getAccessTokenPayloadSecurely()).jwt;
}
```

</TabItem>
</NpmOrScriptTabs>

</TabItem>
<TabItem value="react-native">

```jsx
import SuperTokens from 'supertokens-react-native';

if (await SuperTokens.doesSessionExist()) {
      let userId = await SuperTokens.getUserId();
      let jwt = (await SuperTokens.getAccessTokenPayloadSecurely()).jwt;
}
```
</TabItem>
</FrontendSDKTabs>
