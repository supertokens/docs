---
id: sign-out
title: Sign Out
hide_title: true
---

<!-- COPY DOCS -->
<!-- ./session/common-customizations/sign-out.mdx -->

import FrontendSDKTabs from "/src/components/tabs/FrontendSDKTabs";
import TabItem from '@theme/TabItem';

# Sign out

<FrontendSDKTabs>
<TabItem value="reactjs">

The [`signOut` method](/docs/auth-react/^{docsLinkRecipeName}/sign-out) revokes the session on the frontend and backend.

```jsx
// highlight-next-line
import { signOut } from "supertokens-auth-react/recipe/^{codeImportRecipeName}";

function NavBar () {
  async function onLogout () {
      // highlight-next-line
      await signOut();
      window.location.href = "/";
  }
  return (
    <ul>
         <li>Home</li>
          // highlight-next-line
          <li onClick={onLogout}>Logout</li>

    </ul>
  )
}
```
</TabItem>

<TabItem value="vanillajs">

The [`signOut` method](/docs/website/usage/sign-out) revokes the session for the user.

```jsx
// highlight-next-line
import SuperTokens from "supertokens-website";

async function logout () {
  // highlight-next-line
  await SuperTokens.signOut(); 
  window.location.href = "/";
}
```

</TabItem>
<TabItem value="react-native">

The [`signOut` method](/docs/react-native/usage/sign-out) revokes the session for the user.

```jsx
// highlight-next-line
import SuperTokens from "supertokens-react-native";

async function logout () {
  // highlight-next-line
  await SuperTokens.signOut(); 
  // redirect the user to the login screen
}
```

</TabItem>
</FrontendSDKTabs>

- We do not provide any UI for a Sign-out button
- On success, the `signOut` function does not redirect the user to another page.
