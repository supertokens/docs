---
id: init
title: Init
hide_title: true
---

# Session `init`

```jsx
import SuperTokens from "supertokens-auth-react";
import Session from "supertokens-auth-react/recipe/session";

SuperTokens.init({
    appInfo: {...},
    recipeList: [
        //highlight-next-line
        Session.init({...})
    ]
});
```

:::info
- Learn more about the various config values [here](/docs/website/api-reference).
- All config values except for `apiDomain` and `apiBasePath` are applicable.
:::