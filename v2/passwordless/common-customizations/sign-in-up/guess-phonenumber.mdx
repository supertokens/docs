---
id: guess-phonenumber
title: Guess international phone number
hide_title: true
---

import FrontendSDKTabs from "/src/components/tabs/FrontendSDKTabs"
import TabItem from '@theme/TabItem';

# Guess international phone number

If your users are allowed to log in using both email addresses and phone numbers, then the log in form initially asks the users to enter either. Without the ser may enter a local phone number or one without the appropriate country code. To help the users in this

<FrontendSDKTabs>
<TabItem value="reactjs">

```jsx
SuperTokens.init({
    appInfo: {...},
    recipeList: [
        Passwordless.init({
            // highlight-start
            signInUpFeature: {
                defaultCountry: "HU",
                guessInternationPhoneNumberFromInputPhoneNumber: async (inputPhoneNumber, defaultCountryFromConfig) => {
                    // Return undefined, to prevent the UI from changing to the phone number input.
                    // Return a string to change to the phone number input prefilled with that string.
                    // If it has the country code appropriately filled, the country will also be 
                    // selected from the flag dropdown.
                }
            }
            // highlight-end
        }),
        Session.init({...})
    ]
});
```

</TabItem>
</FrontendSDKTabs>
