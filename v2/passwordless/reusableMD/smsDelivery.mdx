
- Using your Twilio account
- SuperTokens SMS service
- Custom method with full control

By default, if nothing is configured, the SDK will send SMSs using our APIs (`https://api.supertokens.com`). <ins>This is rate limited and is only meant for development / demo purposes.</ins>

**You can learn more about each of these methods in the "SMS Delivery" section (Find it in the navigation list on the left).**