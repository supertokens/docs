
- Configure your SMTP credentials / info
- Custom method with full control

By default, if nothing is configured, the SDK will send emails using our APIs (`https://api.supertokens.com`).

**You can learn more about each of these methods in the "Email Delivery" section under "Auth Flow Customizations" (find it in the navigation list on the left).**