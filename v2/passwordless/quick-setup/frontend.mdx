---
id: frontend
title: Step 1) Frontend
hide_title: true
---

import FrontendSDKTabs from "/src/components/tabs/FrontendSDKTabs"
import TabItem from '@theme/TabItem';
import {Question, Answer} from "/src/components/question"
import AppInfoForm from "/src/components/appInfoForm"

# Frontend Integration 

:::important
For framework specific implementation (like Next.js), please skip this section and go directly to the section with the name of your framework. 
:::

### An example implementation can be [found here](https://github.com/supertokens/supertokens-auth-react/blob/master/examples/with-passwordless/src/App.js).

## 1) Install

<FrontendSDKTabs>
<TabItem value="reactjs">

```bash
npm i -s supertokens-auth-react
```

</TabItem>
<TabItem value="vanillajs">

```bash
npm i -s supertokens-website
```

</TabItem>
</FrontendSDKTabs>

## 2) Call the `init` function

<FrontendSDKTabs>
<TabItem value="reactjs">

<AppInfoForm
    askForAppName={true}
    askForAPIDomain={true}
    askForWebsiteDomain={true}>

<Question
    question="Do you want your users to use magic links or enter codes to sign in?">
<Answer title="Magic links">
In your `App.js` file, import SuperTokens and call the `init` function.

```jsx
import React from 'react';

// highlight-start
import SuperTokens from "supertokens-auth-react";
import Passwordless from "supertokens-auth-react/recipe/passwordless";
import Session from "supertokens-auth-react/recipe/session";

SuperTokens.init({
    appInfo: {
        // learn more about this on https://supertokens.io/docs/passwordless/appinfo
        appName: "^{form_appName}",
        apiDomain: "^{form_apiDomain}",
        websiteDomain: "^{form_websiteDomain}"
    },
    recipeList: [
        Passwordless.init(),
        Session.init()
    ]
});
// highlight-end


/* Your App */
class App extends React.component {
    render() {
       return (...)
    }
}
```
</Answer>
<Answer title="Code">
In your `App.js` file, import SuperTokens and call the `init` function. To use this method you will need to customize the email template on the backend SDK, because the default template only includes the magic link.Please see [this guide](../common-customizations/customize-email) for more details.

```jsx
import React from 'react';

// highlight-start
import SuperTokens from "supertokens-auth-react";
import Passwordless from "supertokens-auth-react/recipe/passwordless";
import Session from "supertokens-auth-react/recipe/session";

SuperTokens.init({
    appInfo: {
        // learn more about this on https://supertokens.io/docs/passwordless/appinfo
        appName: "^{form_appName}",
        apiDomain: "^{form_apiDomain}",
        websiteDomain: "^{form_websiteDomain}"
    },
    recipeList: [
        Passwordless.init({
            // Magic links are enabled by default
            enableMagicLinks: false,
            // The default email template only includes the magic link. Please see "Customize email" in "Common customizations" to include the code as well
            enableCodeFlow: true,
        }),
        Session.init()
    ]
});
// highlight-end


/* Your App */
class App extends React.component {
    render() {
       return (...)
    }
}
```
</Answer>
<Answer title="Both">
In your `App.js` file, import SuperTokens and call the `init` function.

```jsx
import React from 'react';

// highlight-start
import SuperTokens from "supertokens-auth-react";
import Passwordless from "supertokens-auth-react/recipe/passwordless";
import Session from "supertokens-auth-react/recipe/session";

SuperTokens.init({
    appInfo: {
        // learn more about this on https://supertokens.io/docs/passwordless/appinfo
        appName: "^{form_appName}",
        apiDomain: "^{form_apiDomain}",
        websiteDomain: "^{form_websiteDomain}"
    },
    recipeList: [
        Passwordless.init({
            enableMagicLinks: true,
            // The default email template only includes the magic link. Please see "Customize email" in "Common customizations" to include the code as well
            enableCodeFlow: true,
            // Setting displayMagicLinkLanding to true will show a landing page after clicking the magic link instead of immediately signing in and invalidating both the link and the code.
            // This is optional, but it may provide a nicer UX if you are using both magic links and codes.
            displayMagicLinkLanding: true,
        }),
        Session.init()
    ]
});
// highlight-end


/* Your App */
class App extends React.component {
    render() {
       return (...)
    }
}
```
</Answer>
</Question>
</AppInfoForm>

</TabItem>
<TabItem value="vanillajs">

:::caution Not supported
We do not provide a UI for non ReactJS frameworks yet. So you will have to build your own UI, and integrate them with [our APIs](../apis#frontend-driver-interface).
:::

</TabItem>
</FrontendSDKTabs>

## 3) Setup the login UI

<FrontendSDKTabs>
<TabItem value="reactjs">

<Question
    question="Do you use react-router-dom?">
<Answer title="Yes">

Call the `getSuperTokensRoutesForReactRouterDom` method from within any `react-router-dom` `Switch` component.

```jsx
import React from 'react';
import {
  BrowserRouter as Router,
  Switch,
  Route,
  Link
} from "react-router-dom";

// highlight-next-line
import SuperTokens, { getSuperTokensRoutesForReactRouterDom } from "supertokens-auth-react"; 
import Passwordless from "supertokens-auth-react/recipe/passwordless";
import Session from "supertokens-auth-react/recipe/session";

SuperTokens.init({
    // Previously added configs
});


class App extends React.Components {
    render() {
        return (
            <Router>
                <YourNavBar/>
                <Switch>
                    // highlight-next-line
                    {getSuperTokensRoutesForReactRouterDom(require("react-router-dom"))}
                    {...}
                </Switch>
                <YourFooter />
            </Router>
        );
    }
}
```

</Answer>
<Answer title="No">

Add the highlighted code snippet to your root level `render` function.

```jsx
import React from 'react';
import SuperTokens from "supertokens-auth-react";

class App extends React.Components {
    render() {
        // highlight-start
        if (SuperTokens.canHandleRoute()) {
            return SuperTokens.getRoutingComponent()
        }
        // highlight-end
        
        return (
            (...)
        );
    }

}
```

</Answer>
</Question>
</TabItem>
<TabItem value="vanillajs">

:::caution Not supported
We do not provide a UI for non ReactJS frameworks yet. So you will have to build your own UI, and integrate them with [our APIs](../apis#frontend-driver-interface).
:::

</TabItem>
</FrontendSDKTabs>

## 4) Add API interceptors for automatic session refreshing

<Question
    question="Do you use axios on your frontend?">
<Answer title="Yes">

:::important
You must call `addAxiosInterceptors` on all `axios` imports.
:::

<FrontendSDKTabs>
<TabItem value="reactjs">

```jsx
import axios from "axios";
// highlight-start
import Session from "supertokens-auth-react/recipe/session";
Session.addAxiosInterceptors(axios);
// highlight-end

async function callAPI() {
    // use axios as you normally do
    let response = await axios.get(...);
}
```

</TabItem>
<TabItem value="vanillajs">

```jsx
import axios from "axios";
// highlight-start
import SuperTokens from 'supertokens-website';
SuperTokens.addAxiosInterceptors(axios);
// highlight-end

async function callAPI() {
    // use axios as you normally do
    let response = await axios.get(...);
}
```

</TabItem>
</FrontendSDKTabs>

</Answer>
<Answer title="No">

:::success
Interceptors are added automatically. No action needed here.
:::

</Answer>
</Question>

## 5) Visit `/auth` to see the login UI (ReactJS only)

<img width="300px" src="/img/passwordless/signup-light.png" />

At this stage, you've successfully integrated your website with SuperTokens. The next section will guide you through setting up your backend.
