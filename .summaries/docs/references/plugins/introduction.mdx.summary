## Summary
This documentation introduces SuperTokens plugins as an extension system that adds functionality to authentication flows without modifying core recipes, currently supporting only React and Node.js SDKs and working through the override mechanism to inject custom features.

## Keywords
- SuperTokens plugins
- authentication extension system
- React SDK plugins
- Node.js SDK plugins
- override mechanism
- captcha-react plugin
- captcha-nodejs plugin
- user-banning plugins
- tenant-discovery plugins
- opentelemetry-nodejs plugin
- progressive-profiling plugins
- function overrides
- component overrides
- API overrides
- supertokens-plugins repository
- authentication recipes
- frontend function overrides
- backend function overrides
- React component overrides