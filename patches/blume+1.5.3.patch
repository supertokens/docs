diff --git a/node_modules/blume/dist/cli/index.js b/node_modules/blume/dist/cli/index.js
index 86b5ac9..3f17a89 100755
--- a/node_modules/blume/dist/cli/index.js
+++ b/node_modules/blume/dist/cli/index.js
@@ -3269,7 +3269,7 @@ var buildApiCatalog = (config) => {
-
+
 // src/ai/link-headers.ts
 var buildHomeLinkHeader = (config, routePaths) => {
-  const deployBase = normalizeBasePath(config.deployment.base);
+  const deployBase = `${normalizeBasePath(config.deployment.base)}${config.basePath}`;
   const links = [];
   if (hasApiCatalog(config)) {
     links.push(`<${deployBase}${API_CATALOG_PATH}>; rel="api-catalog"; type="application/linkset+json"`);
@@ -3792,7 +3792,8 @@ var renderIntegrationBridge = (bridge) => {
       configSourceMarker: "",
       userConfigImports: "",
       userConfigSetup: "",
-      userIntegrationSpread: ""
+      userIntegrationSpread: "",
+      userVitePluginSpread: ""
     };
   }
   return {
@@ -3806,7 +3807,8 @@ import { createModuleLoader } from "blume/core/load-module.ts";
 const blumeConfig = await loadBlumeConfig(resolve(dirname(fileURLToPath(import.meta.url)), ${JSON.stringify(bridge.configFile)}));
-
+
 `,
-    userIntegrationSpread: ", ...(blumeConfig?.integrations ?? [])"
+    userIntegrationSpread: ", ...(blumeConfig?.integrations ?? [])",
+    userVitePluginSpread: ", ...(blumeConfig?.vite?.plugins ?? [])"
   };
 };
 var renderImageOption = (config) => config.image.domains.length > 0 || config.image.remotePatterns.length > 0 ? `
@@ -3866,6 +3868,8 @@ var astroConfigTemplate = (options) => {
   site: ${JSON.stringify(deployment.site)},` : "";
   const baseOption = deployment.base ? `
   base: ${JSON.stringify(deployment.base)},` : "";
+  const assetsOption = config.publicAssetBasePath ? `
+  build: { assets: ${JSON.stringify(config.publicAssetBasePath.slice(1))} },` : "";
   const imageOption = renderImageOption(config);
   const i18nOption = config.i18n ? `
   i18n: ${JSON.stringify({
@@ -3946,7 +3950,8 @@ var astroConfigTemplate = (options) => {
     configSourceMarker,
     userConfigImports,
     userConfigSetup,
-    userIntegrationSpread
+    userIntegrationSpread,
+    userVitePluginSpread
   } = renderIntegrationBridge(options.integrationBridge);
   return `// Generated by Blume. Do not edit; this file is recreated on each run.
 ${configSourceMarker}${userConfigImports}${defineConfigImport}
@@ -3959,7 +3964,7 @@ ${userConfigSetup}export default defineConfig({
   srcDir: ${JSON.stringify(`${context.outDir}/src`)},
   outDir: ${JSON.stringify(astroOutDir(context))},
   publicDir: ${JSON.stringify(`${context.root}/public`)},
-  output: ${JSON.stringify(deployment.output)},${adapterOption}${sessionOption}${siteOption}${baseOption}${imageOption}${redirectsOption}${i18nOption}${fontsOption}
+  output: ${JSON.stringify(deployment.output)},${adapterOption}${sessionOption}${siteOption}${baseOption}${assetsOption}${imageOption}${redirectsOption}${i18nOption}${fontsOption}
   integrations: [${integrations.join(", ")}${userIntegrationSpread}],
   markdown: {
     processor: blumeMarkdownProcessor(${JSON.stringify({
@@ -3984,7 +3989,7 @@ ${userConfigSetup}export default defineConfig({
   // instantly.
   prefetch: { prefetchAll: true },
   vite: {
-    plugins: [tailwindcss(), prerenderDepsPlugin(), serverAppResolvePlugin()],
+    plugins: [tailwindcss(), prerenderDepsPlugin(), serverAppResolvePlugin()${userVitePluginSpread}],
     // Everything hydration can reach must be part of the dev dep optimizer's
     // FIRST run. The Vite root is the generated runtime, so user pages,
     // islands, and aliased components live outside it and are only crawled
@@ -10042,6 +10047,7 @@ var publicJwkSchema = z2.record(z2.string(), z2.unknown()).superRefine((jwk, ctx
   }
 });
 var mcpConfigSchema = z2.strictObject({
+  discovery: z2.boolean().default(true),
   enabled: z2.boolean().default(false),
   instructions: z2.string().optional(),
   name: z2.string().optional(),
@@ -10434,6 +10440,7 @@ var blumeConfigSchema = z2.strictObject({
   banner: bannerConfigSchema.optional(),
   basePath: z2.string().optional().transform((value) => normalizeBasePath(value)),
   content: contentConfigSchema.prefault({}),
+  publicAssetBasePath: z2.string().optional().transform((value) => normalizeBasePath(value)),
   dateFormat: dateFormatConfigSchema.default({ dateStyle: "long" }),
   deployment: deploymentConfigSchema.prefault({}),
   description: z2.string().optional(),
@@ -10457,6 +10464,7 @@ var blumeConfigSchema = z2.strictObject({
   theme: themeConfigSchema.prefault({}),
   title: z2.string().default("Documentation"),
   toc: tocConfigSchema,
+  vite: z2.strictObject({ plugins: z2.array(z2.custom()).default([]) }).prefault({}),
   versions: versionsConfigSchema.optional()
 }).superRefine((config, ctx) => {
   if (config.versions && config.i18n) {
@@ -14539,7 +14547,7 @@ var buildAgentReadability = (project) => {
     return null;
   }
   const site = config.deployment.site ?? null;
-  const deployBase = normalizeBasePath(config.deployment.base);
+  const deployBase = `${normalizeBasePath(config.deployment.base)}${config.basePath}`;
   const abs = (path) => {
     const based = withBasePath(deployBase, path);
     return site ? absoluteUrl(site, based) : based;
@@ -14553,7 +14561,7 @@ var buildAgentReadability = (project) => {
   }
   if (config.ai.mcp.enabled) {
     artifacts.mcp = {
-      discovery: abs("/.well-known/mcp.json"),
+      ...config.ai.mcp.discovery ? { discovery: abs("/.well-known/mcp.json") } : {},
       url: abs(config.ai.mcp.route)
     };
   }
@@ -18706,7 +18714,7 @@ var detectUsesMath = async (root, staged = []) => {
 };
 var hashConfigSource = (source) => createHash4("sha256").update(source).digest("hex");
 var loadIntegrationBridge = async (config, context) => {
-  if (config.integrations.length === 0 || !context.configFile) {
+  if (config.integrations.length === 0 && config.vite.plugins.length === 0 || !context.configFile) {
     return;
   }
   return {
@@ -18843,11 +18851,12 @@ var APPLE_ICON_CANDIDATES = [
   "apple-icon.jpeg",
   "apple-touch-icon.png"
 ];
+var publicAssetHref = (basePath, name) => `${basePath ?? ""}/${name}`;
 var resolveIconFile = (project, candidates) => {
   const { root } = project.context;
   for (const name of candidates) {
     if (existsSync15(join28(root, "public", name))) {
-      return { href: `/${name}`, type: faviconType(name) };
+      return { href: publicAssetHref(project.config.publicAssetBasePath, name), type: faviconType(name) };
     }
   }
   for (const name of candidates) {
@@ -18865,7 +18874,7 @@ var resolveFavicon = (project) => {
     if (existsSync15(join28(root, "public", name))) {
       const type = faviconType(name);
       const sibling = darkSibling(name);
-      return existsSync15(join28(root, "public", sibling)) ? { dark: { href: `/${sibling}`, type }, href: `/${name}`, type } : { href: `/${name}`, type };
+      return existsSync15(join28(root, "public", sibling)) ? { dark: { href: publicAssetHref(project.config.publicAssetBasePath, sibling), type }, href: publicAssetHref(project.config.publicAssetBasePath, name), type } : { href: publicAssetHref(project.config.publicAssetBasePath, name), type };
     }
   }
   for (const name of FAVICON_CANDIDATES) {
@@ -18971,7 +18980,7 @@ var buildRuntimeData = (project) => {
       analytics: config.analytics ?? null,
       appleIcon: resolveAppleIcon(project),
       ask: config.ai.ask?.enabled ? {
-        endpoint: config.ai.ask.endpoint ?? null,
+        endpoint: config.ai.ask.endpoint ?? withBasePath(config.basePath, "/api/ask"),
         suggestions: config.ai.ask.suggestions
       } : null,
       banner: resolveBanner(config),
@@ -19090,7 +19099,7 @@ var planMcp = (project, srcDir, userPages) => {
   }
   return {
     ...base,
-    discoveryPages: [
+    discoveryPages: config.ai.mcp.discovery ? [
       {
         entrypoint: join28(dir, "discovery.ts"),
         pattern: "/.well-known/mcp.json"
@@ -19099,7 +19108,7 @@ var planMcp = (project, srcDir, userPages) => {
         entrypoint: join28(dir, "server-card.ts"),
         pattern: "/.well-known/mcp/server-card.json"
       }
-    ],
+    ] : [],
     enabled: true
   };
 };
@@ -19119,8 +19128,10 @@ var writeMcpFiles = async (project, plan, write) => {
     write(join28(plan.srcDir, "generated", "mcp-data.json"), `${JSON.stringify(data)}
 `),
     write(join28(plan.srcDir, "pages", mcpPageFile(plan.route)), mcpEndpointTemplate(plan.route)),
-    write(join28(plan.dir, "discovery.ts"), staticJsonEndpointTemplate(buildMcpDiscovery(discoveryInput))),
-    write(join28(plan.dir, "server-card.ts"), staticJsonEndpointTemplate(buildMcpServerCard(discoveryInput)))
+    ...project.config.ai.mcp.discovery ? [
+      write(join28(plan.dir, "discovery.ts"), staticJsonEndpointTemplate(buildMcpDiscovery(discoveryInput))),
+      write(join28(plan.dir, "server-card.ts"), staticJsonEndpointTemplate(buildMcpServerCard(discoveryInput)))
+    ] : []
   ]);
 };
 var planPlaygroundProxy = (config, srcDir) => ({
diff --git a/node_modules/blume/dist/types/core/config-input.d.ts b/node_modules/blume/dist/types/core/config-input.d.ts
index 6084a0c..9fa2792 100644
--- a/node_modules/blume/dist/types/core/config-input.d.ts
+++ b/node_modules/blume/dist/types/core/config-input.d.ts
@@ -620,6 +620,8 @@ export interface LlmsTxtConfig {
 }
 /** Expose the docs as an MCP server for connecting agents. */
 export interface McpConfig {
+    /** Emit MCP discovery under `/.well-known`. Defaults to `true`. */
+    discovery?: boolean;
     /** Turn the MCP server on. Defaults to `false`. */
     enabled?: boolean;
     /** Optional system hint passed to connecting agents. */
@@ -1252,6 +1254,8 @@ export interface BlumeConfig {
     basePath?: string;
     /** Where content lives and how it's discovered. */
     content?: ContentConfig;
+    /** Public-file URL prefix for microfrontend-owned assets. */
+    publicAssetBasePath?: string;
     /**
      * Date presentation for the "last updated" stamp and the changelog timeline.
      * Pass-through `Intl.DateTimeFormat` options; defaults to `{ dateStyle: "long" }`.
@@ -1309,6 +1313,12 @@ export interface BlumeConfig {
     title?: string;
     /** On-page table of contents. Defaults to on (H2–H3). */
     toc?: TocConfig;
+    /** Vite plugins appended after Blume's built-ins. */
+    vite?: {
+        plugins?: {
+            name: string;
+        }[];
+    };
     /** Docs versioning (opt-in frozen snapshots with a version switcher). */
     versions?: VersionsConfig;
 }
diff --git a/node_modules/blume/src/ai/agent-readability.ts b/node_modules/blume/src/ai/agent-readability.ts
index e28fba2..562d9b1 100644
--- a/node_modules/blume/src/ai/agent-readability.ts
+++ b/node_modules/blume/src/ai/agent-readability.ts
@@ -59,7 +59,7 @@ interface AgentArtifacts extends WellKnownArtifacts {
   markdown: { contentNegotiation?: string; pattern: string };
   llmsFullTxt?: string;
   llmsTxt?: string;
-  mcp?: { discovery: string; url: string };
+  mcp?: { discovery?: string; url: string };
   askApi?: string;
   sitemap?: string;
   feeds?: string[];
@@ -115,7 +115,7 @@ export const buildAgentReadability = (
   const site = config.deployment.site ?? null;
   // Every artifact is served under `deployment.base` — with or without a
   // `site`; concatenate rather than `new URL()` so the subpath is preserved.
-  const deployBase = normalizeBasePath(config.deployment.base);
+  const deployBase = `${normalizeBasePath(config.deployment.base)}${config.basePath}`;
   const abs = (path: string): string => {
     const based = withBasePath(deployBase, path);
     return site ? absoluteUrl(site, based) : based;
@@ -142,7 +142,9 @@ export const buildAgentReadability = (
   }
   if (config.ai.mcp.enabled) {
     artifacts.mcp = {
-      discovery: abs("/.well-known/mcp.json"),
+      ...(config.ai.mcp.discovery
+        ? { discovery: abs("/.well-known/mcp.json") }
+        : {}),
       url: abs(config.ai.mcp.route),
     };
   }
diff --git a/node_modules/blume/src/ai/link-headers.ts b/node_modules/blume/src/ai/link-headers.ts
index 9a146e3..6eec17e 100644
--- a/node_modules/blume/src/ai/link-headers.ts
+++ b/node_modules/blume/src/ai/link-headers.ts
@@ -26,7 +26,7 @@ export const buildHomeLinkHeader = (
   config: ResolvedConfig,
   routePaths: readonly string[]
 ): string | null => {
-  const deployBase = normalizeBasePath(config.deployment.base);
+  const deployBase = `${normalizeBasePath(config.deployment.base)}${config.basePath}`;
   const links: string[] = [];
   // RFC 9727 §3: the api-catalog relation is how a homepage advertises the
   // well-known catalog.
diff --git a/node_modules/blume/src/ai/mcp/server.ts b/node_modules/blume/src/ai/mcp/server.ts
index bf13cb5..35e122c 100644
--- a/node_modules/blume/src/ai/mcp/server.ts
+++ b/node_modules/blume/src/ai/mcp/server.ts
@@ -38,10 +38,29 @@ const CORS_HEADERS = {
   "Access-Control-Allow-Headers":
     "Content-Type, Mcp-Session-Id, Mcp-Protocol-Version",
   "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
-  "Access-Control-Allow-Origin": "*",
   "Access-Control-Expose-Headers": "Mcp-Session-Id",
 };
-
+
+const appendVaryOrigin = (headers: Headers): void => {
+  const vary = headers.get("Vary");
+  if (!vary?.split(",").some((value) => value.trim().toLowerCase() === "origin")) {
+    headers.set("Vary", vary ? `${vary}, Origin` : "Origin");
+  }
+};
+
+const corsHeaders = (headers: Headers, origin: string | null): Headers => {
+  for (const [key, value] of Object.entries(CORS_HEADERS)) {
+    headers.set(key, value);
+  }
+  if (origin !== null) {
+    headers.set("Access-Control-Allow-Origin", origin);
+    appendVaryOrigin(headers);
+  } else {
+    headers.delete("Access-Control-Allow-Origin");
+  }
+  return headers;
+};
+
 // Each field is a preprocess pipe: the input side accepts the sloppy shapes
 // LLM callers actually send (a bare string for an array field, `[]`/`{}`
 // meaning "no filter", out-of-range limits clamped rather than rejected), and
@@ -476,13 +495,26 @@ export const createMcpFetchHandler = (
   const index = createIndexProvider(data.documents, data.defaultLocale);
-
+
   return async (request: Request): Promise<Response> => {
+    const origin = request.headers.get("Origin");
+    if (origin !== null && origin !== new URL(request.url).origin) {
+      const headers = new Headers();
+      appendVaryOrigin(headers);
+      return new Response("Forbidden", { headers, status: 403 });
+    }
+
     if (request.method === "OPTIONS") {
-      return new Response(null, { headers: CORS_HEADERS, status: 204 });
+      return new Response(null, {
+        headers: corsHeaders(new Headers(), origin),
+        status: 204,
+      });
     }
     if (request.method === "GET") {
       // No server-initiated streams are needed for read-only tools.
       return new Response("Method Not Allowed", {
-        headers: { ...CORS_HEADERS, Allow: "POST, OPTIONS" },
+        headers: corsHeaders(
+          new Headers({ Allow: "POST, OPTIONS" }),
+          origin
+        ),
         status: 405,
       });
     }
@@ -498,10 +530,7 @@ export const createMcpFetchHandler = (
     await server.connect(transport);
     const response = await transport.handleRequest(request);
-
+
-    const headers = new Headers(response.headers);
-    for (const [key, value] of Object.entries(CORS_HEADERS)) {
-      headers.set(key, value);
-    }
+    const headers = corsHeaders(new Headers(response.headers), origin);
     return new Response(response.body, {
       headers,
       status: response.status,
diff --git a/node_modules/blume/src/astro/generate.ts b/node_modules/blume/src/astro/generate.ts
index 185a200..dd9a740 100644
--- a/node_modules/blume/src/astro/generate.ts
+++ b/node_modules/blume/src/astro/generate.ts
@@ -22,7 +22,7 @@ import { askBackendRuntimeDep, resolveAskBackend } from "../ai/ask.ts";
 import { buildRawMarkdown, markdownRoutePaths } from "../ai/markdown.ts";
 import { buildMcpData } from "../ai/mcp/data.ts";
 import { buildMcpDiscovery, buildMcpServerCard } from "../ai/mcp/discovery.ts";
-import { normalizeBasePath } from "../core/base-path.ts";
+import { normalizeBasePath, withBasePath } from "../core/base-path.ts";
 import { validateUsedComponents } from "../core/component-diagnostics.ts";
 import { analyzeComponentOverrides } from "../core/component-overrides.ts";
 import { collectContentAssets } from "../core/content-assets.ts";
@@ -777,7 +777,10 @@ const loadIntegrationBridge = async (
   config: ResolvedConfig,
   context: BlumeProject["context"]
 ): Promise<Parameters<typeof astroConfigTemplate>[0]["integrationBridge"]> => {
-  if (config.integrations.length === 0 || !context.configFile) {
+  if (
+    (config.integrations.length === 0 && config.vite.plugins.length === 0) ||
+    !context.configFile
+  ) {
     return;
   }
   return {
@@ -1034,7 +1037,10 @@ const resolveIconFile = (
   const { root } = project.context;
   for (const name of candidates) {
     if (existsSync(join(root, "public", name))) {
-      return { href: `/${name}`, type: faviconType(name) };
+      return {
+        href: publicAssetHref(project.config.publicAssetBasePath, name),
+        type: faviconType(name),
+      };
     }
   }
   for (const name of candidates) {
@@ -1047,6 +1053,9 @@ const resolveIconFile = (
   return null;
 };
-
+
+export const publicAssetHref = (basePath: string | undefined, name: string): string =>
+  `${basePath ?? ""}/${name}`;
+
 /**
  * Resolve the site favicon by convention, falling back to the bundled Blume mark
  * when the project ships no `icon.*`/`favicon.*` file. The dark-scheme variant
@@ -1063,8 +1072,15 @@ const resolveFavicon = (project: BlumeProject): BlumeFavicon => {
       const type = faviconType(name);
       const sibling = darkSibling(name);
       return existsSync(join(root, "public", sibling))
-        ? { dark: { href: `/${sibling}`, type }, href: `/${name}`, type }
-        : { href: `/${name}`, type };
+        ? {
+            dark: {
+              href: publicAssetHref(project.config.publicAssetBasePath, sibling),
+              type,
+            },
+            href: publicAssetHref(project.config.publicAssetBasePath, name),
+            type,
+          }
+        : { href: publicAssetHref(project.config.publicAssetBasePath, name), type };
     }
   }
   for (const name of FAVICON_CANDIDATES) {
@@ -1237,7 +1253,8 @@ export const buildRuntimeData = (project: BlumeProject): string => {
       appleIcon: resolveAppleIcon(project),
       ask: config.ai.ask?.enabled
         ? {
-            endpoint: config.ai.ask.endpoint ?? null,
+            endpoint:
+              config.ai.ask.endpoint ?? withBasePath(config.basePath, "/api/ask"),
             suggestions: config.ai.ask.suggestions,
           }
         : null,
@@ -1400,16 +1417,18 @@ const planMcp = (
   }
   return {
     ...base,
-    discoveryPages: [
-      {
-        entrypoint: join(dir, "discovery.ts"),
-        pattern: "/.well-known/mcp.json",
-      },
-      {
-        entrypoint: join(dir, "server-card.ts"),
-        pattern: "/.well-known/mcp/server-card.json",
-      },
-    ],
+    discoveryPages: config.ai.mcp.discovery
+      ? [
+          {
+            entrypoint: join(dir, "discovery.ts"),
+            pattern: "/.well-known/mcp.json",
+          },
+          {
+            entrypoint: join(dir, "server-card.ts"),
+            pattern: "/.well-known/mcp/server-card.json",
+          },
+        ]
+      : [],
     enabled: true,
   };
 };
@@ -1440,14 +1459,18 @@ const writeMcpFiles = async (
       join(plan.srcDir, "pages", mcpPageFile(plan.route)),
       mcpEndpointTemplate(plan.route)
     ),
-    write(
-      join(plan.dir, "discovery.ts"),
-      staticJsonEndpointTemplate(buildMcpDiscovery(discoveryInput))
-    ),
-    write(
-      join(plan.dir, "server-card.ts"),
-      staticJsonEndpointTemplate(buildMcpServerCard(discoveryInput))
-    ),
+    ...(project.config.ai.mcp.discovery
+      ? [
+          write(
+            join(plan.dir, "discovery.ts"),
+            staticJsonEndpointTemplate(buildMcpDiscovery(discoveryInput))
+          ),
+          write(
+            join(plan.dir, "server-card.ts"),
+            staticJsonEndpointTemplate(buildMcpServerCard(discoveryInput))
+          ),
+        ]
+      : []),
   ]);
 };
-
+
diff --git a/node_modules/blume/src/astro/templates.ts b/node_modules/blume/src/astro/templates.ts
index d4656c7..af2e29e 100644
--- a/node_modules/blume/src/astro/templates.ts
+++ b/node_modules/blume/src/astro/templates.ts
@@ -352,6 +352,7 @@ const renderIntegrationBridge = (
       userConfigImports: "",
       userConfigSetup: "",
       userIntegrationSpread: "",
+      userVitePluginSpread: "",
     };
   }
   return {
@@ -363,6 +364,7 @@ const renderIntegrationBridge = (
       bridge.configFile
     )}));\n\n`,
     userIntegrationSpread: ", ...(blumeConfig?.integrations ?? [])",
+    userVitePluginSpread: ", ...(blumeConfig?.vite?.plugins ?? [])",
   };
 };
-
+
@@ -513,6 +515,9 @@ export const astroConfigTemplate = (options: {
   const baseOption = deployment.base
     ? `\n  base: ${JSON.stringify(deployment.base)},`
     : "";
+  const assetsOption = config.publicAssetBasePath
+    ? `\n  build: { assets: ${JSON.stringify(config.publicAssetBasePath.slice(1))} },`
+    : "";
   const imageOption = renderImageOption(config);
-
+
   // Astro's native i18n gives locale-aware helpers + `<html lang>` correctness.
@@ -666,6 +671,7 @@ export const astroConfigTemplate = (options: {
     userConfigImports,
     userConfigSetup,
     userIntegrationSpread,
+    userVitePluginSpread,
   } = renderIntegrationBridge(options.integrationBridge);
-
+
   return `// Generated by Blume. Do not edit; this file is recreated on each run.
@@ -679,7 +685,7 @@ ${userConfigSetup}export default defineConfig({
   srcDir: ${JSON.stringify(`${context.outDir}/src`)},
   outDir: ${JSON.stringify(astroOutDir(context))},
   publicDir: ${JSON.stringify(`${context.root}/public`)},
-  output: ${JSON.stringify(deployment.output)},${adapterOption}${sessionOption}${siteOption}${baseOption}${imageOption}${redirectsOption}${i18nOption}${fontsOption}
+  output: ${JSON.stringify(deployment.output)},${adapterOption}${sessionOption}${siteOption}${baseOption}${assetsOption}${imageOption}${redirectsOption}${i18nOption}${fontsOption}
   integrations: [${integrations.join(", ")}${userIntegrationSpread}],
   markdown: {
     processor: blumeMarkdownProcessor(${JSON.stringify({
@@ -706,7 +712,7 @@ ${userConfigSetup}export default defineConfig({
   // instantly.
   prefetch: { prefetchAll: true },
   vite: {
-    plugins: [tailwindcss(), prerenderDepsPlugin(), serverAppResolvePlugin()],
+    plugins: [tailwindcss(), prerenderDepsPlugin(), serverAppResolvePlugin()${userVitePluginSpread}],
     // Everything hydration can reach must be part of the dev dep optimizer's
     // FIRST run. The Vite root is the generated runtime, so user pages,
     // islands, and aliased components live outside it and are only crawled
diff --git a/node_modules/blume/src/components/content/CodeGroup.astro b/node_modules/blume/src/components/content/CodeGroup.astro
index d9e7412..838dc06 100644
--- a/node_modules/blume/src/components/content/CodeGroup.astro
+++ b/node_modules/blume/src/components/content/CodeGroup.astro
@@ -3,11 +3,12 @@ import Tabs from "./Tabs.astro";
 
 interface Props {
   dropdown?: boolean;
+  tabValues?: string;
 }
 
-const { dropdown = false } = Astro.props;
+const { dropdown = false, tabValues } = Astro.props;
 ---
 
-<Tabs dropdown={dropdown} hash={false}>
+<Tabs dropdown={dropdown} hash={false} tabValues={tabValues}>
   <slot />
 </Tabs>
diff --git a/node_modules/blume/src/components/content/Tabs.astro b/node_modules/blume/src/components/content/Tabs.astro
index 1b1d34e..aa5651b 100644
--- a/node_modules/blume/src/components/content/Tabs.astro
+++ b/node_modules/blume/src/components/content/Tabs.astro
@@ -19,6 +19,7 @@ interface Props {
    */
   param?: string;
   sync?: boolean;
+  tabValues?: string;
 }
 
 const {
@@ -29,6 +30,7 @@ const {
   inline = false,
   param,
   sync = true,
+  tabValues,
 } = Astro.props;
 
 // MDX string attributes (`hash="false"` from generated markup) must read as
@@ -52,6 +54,7 @@ const useDropdown = dropdown && !inline;
   data-hash={hashEnabled ? "true" : "false"}
   data-param={param}
   data-sync={sync ? "true" : "false"}
+  data-tab-values={tabValues}
 >
   <div
     class:list={[
@@ -132,10 +135,37 @@ const useDropdown = dropdown && !inline;
                 child instanceof HTMLElement && child.matches("pre, figure")
             )
           : [];
-        for (const block of blocks) {
-          block.setAttribute("data-blume-tab-panel", "");
+        if (this.dataset.tabValues) {
+          let tabValues: Record<string, string> = {};
+          try {
+            tabValues = JSON.parse(this.dataset.tabValues);
+          } catch {
+            this.dataset.codeGroupInvalid = "true";
+          }
+          const grouped = new Map<string, HTMLElement>();
+          for (const block of blocks) {
+            const title = block.dataset.title ?? "Example";
+            const value = tabValues[title];
+            const key = value ?? title;
+            if (!value) this.dataset.codeGroupInvalid = "true";
+            let panel = grouped.get(key);
+            if (!panel) {
+              panel = document.createElement("div");
+              panel.dataset.blumeTabPanel = "";
+              panel.dataset.title = title;
+              if (value) panel.dataset.tabId = value;
+              wrapper?.insertBefore(panel, block);
+              grouped.set(key, panel);
+            }
+            panel.appendChild(block);
+          }
+          panels = [...grouped.values()];
+        } else {
+          for (const block of blocks) {
+            block.setAttribute("data-blume-tab-panel", "");
+          }
+          panels = blocks;
         }
-        panels = blocks;
       }
       if (!list || panels.length === 0) {
         return;
diff --git a/node_modules/blume/src/components/layout/RootLayout.astro b/node_modules/blume/src/components/layout/RootLayout.astro
index 017733b..94c9d15 100644
--- a/node_modules/blume/src/components/layout/RootLayout.astro
+++ b/node_modules/blume/src/components/layout/RootLayout.astro
@@ -489,7 +489,7 @@ const bannerKey = banner?.dismissible ? banner.key : null;
     {
       discovery?.agentReadability && (
         <link
-          href={withBase("/agent-readability.json")}
+          href={withBase(`${clientData?.config.basePath ?? ""}/agent-readability.json`)}
           rel="describedby"
           type="application/json"
         />
@@ -497,7 +497,11 @@ const bannerKey = banner?.dismissible ? banner.key : null;
     }
     {
       discovery?.llmsTxt && (
-        <link href={withBase("/llms.txt")} rel="describedby" type="text/plain" />
+        <link
+          href={withBase(`${clientData?.config.basePath ?? ""}/llms.txt`)}
+          rel="describedby"
+          type="text/plain"
+        />
       )
     }
     {
diff --git a/node_modules/blume/src/core/config-input.ts b/node_modules/blume/src/core/config-input.ts
index dc11a82..4e598b8 100644
--- a/node_modules/blume/src/core/config-input.ts
+++ b/node_modules/blume/src/core/config-input.ts
@@ -713,6 +713,8 @@ export interface LlmsTxtConfig {
-
+
 /** Expose the docs as an MCP server for connecting agents. */
 export interface McpConfig {
+  /** Emit MCP discovery under `/.well-known`. Defaults to `true`. */
+  discovery?: boolean;
   /** Turn the MCP server on. Defaults to `false`. */
   enabled?: boolean;
   /** Optional system hint passed to connecting agents. */
@@ -1429,6 +1431,8 @@ export interface BlumeConfig {
   basePath?: string;
   /** Where content lives and how it's discovered. */
   content?: ContentConfig;
+  /** Public-file URL prefix for microfrontend-owned assets. */
+  publicAssetBasePath?: string;
   /**
    * Date presentation for the "last updated" stamp and the changelog timeline.
    * Pass-through `Intl.DateTimeFormat` options; defaults to `{ dateStyle: "long" }`.
@@ -1486,6 +1490,10 @@ export interface BlumeConfig {
   title?: string;
   /** On-page table of contents. Defaults to on (H2–H3). */
   toc?: TocConfig;
+  /** Vite plugins appended after Blume's built-ins. */
+  vite?: {
+    plugins?: { name: string }[];
+  };
   /** Docs versioning (opt-in frozen snapshots with a version switcher). */
   versions?: VersionsConfig;
 }
diff --git a/node_modules/blume/src/core/schema.ts b/node_modules/blume/src/core/schema.ts
index 476af6f..5db7079 100644
--- a/node_modules/blume/src/core/schema.ts
+++ b/node_modules/blume/src/core/schema.ts
@@ -760,6 +760,7 @@ const publicJwkSchema = z
   });
-
+
 const mcpConfigSchema = z.strictObject({
+  discovery: z.boolean().default(true),
   enabled: z.boolean().default(false),
   /** Optional system hint passed to connecting agents. */
   instructions: z.string().optional(),
@@ -1716,6 +1717,10 @@ export const blumeConfigSchema = z
       .optional()
       .transform((value) => normalizeBasePath(value)),
     content: contentConfigSchema.prefault({}),
+    publicAssetBasePath: z
+      .string()
+      .optional()
+      .transform((value) => normalizeBasePath(value)),
     /**
      * Date presentation for the "last updated" stamp and the changelog timeline.
      * Pass-through `Intl.DateTimeFormat` options; defaults to `{ dateStyle: "long" }`.
@@ -1751,6 +1756,11 @@ export const blumeConfigSchema = z
     theme: themeConfigSchema.prefault({}),
     title: z.string().default("Documentation"),
     toc: tocConfigSchema,
+    vite: z
+      .strictObject({
+        plugins: z.array(z.custom<{ name: string }>()).default([]),
+      })
+      .prefault({}),
     versions: versionsConfigSchema.optional(),
   })
   .superRefine((config, ctx) => {
diff --git a/node_modules/blume/src/markdown/code-title.ts b/node_modules/blume/src/markdown/code-title.ts
index 9e8245d..12a349e 100644
--- a/node_modules/blume/src/markdown/code-title.ts
+++ b/node_modules/blume/src/markdown/code-title.ts
@@ -30,6 +30,7 @@ export interface CodeTitleTransformer {
 // `subtitle="..."` (or any `*title=` attr) from reading as a title.
 const TITLE_ATTR = /(?:^|\s)title=(?:"(?<dq>[^"]*)"|'(?<sq>[^']*)')/u;
 const LINE_NUMBERS = /(?:^|\s)lineNumbers(?=\s|$)/u;
+const OPTION_ATTR = /(?:^|\s)option=(?:"(?<dq>[^"]*)"|'(?<sq>[^']*)')/u;
 // Any quoted `key="..."` attr — blanked before keyword/bare-token scans so a
 // quoted value can't leak tokens (`title="enable lineNumbers later"`).
 const QUOTED_ATTR = /[\w-]+=(?:"[^"]*"|'[^']*')/gu;
@@ -68,6 +69,11 @@ const parseTitle = (raw: string | undefined): string | undefined => {
 const hasLineNumbers = (raw: string | undefined): boolean =>
   Boolean(raw && LINE_NUMBERS.test(withoutQuotedAttrs(raw)));
 
+const parseOption = (raw: string | undefined): string | undefined => {
+  const match = raw?.match(OPTION_ATTR);
+  return match?.groups?.dq ?? match?.groups?.sq;
+};
+
 /** Build the transformer. Runs after Shiki's built-in `data-language` hook. */
 export const codeTitleTransformer = (): CodeTitleTransformer => ({
   name: "blume:code-meta",
@@ -80,5 +86,9 @@ export const codeTitleTransformer = (): CodeTitleTransformer => ({
     if (hasLineNumbers(raw)) {
       node.properties.dataLineNumbers = true;
     }
+    const option = parseOption(raw);
+    if (option) {
+      node.properties.dataCodeOption = option;
+    }
   },
 });
diff --git a/node_modules/blume/src/markdown/index.ts b/node_modules/blume/src/markdown/index.ts
index 981debb..7c11602 100644
--- a/node_modules/blume/src/markdown/index.ts
+++ b/node_modules/blume/src/markdown/index.ts
@@ -200,6 +200,8 @@ export interface HighlightCodeOptions extends BlumeShikiOptions {
   themes?: CodeThemes;
   /** Header title (a filename), matching a fence's `title="..."` meta. */
   title?: string;
+  /** Raw fence metadata for callers rendering a migrated Markdown fence. */
+  meta?: string;
 }
-
+
 /**
@@ -224,9 +226,11 @@ export const highlightCode = async (
       // The code-title transformer reads the fence meta; feeding a `title="..."`
       // string here gives non-fence callers (e.g. `<CodeBlock title>`) the same
       // `data-title` header a Markdown fence gets.
-      meta: options.title
-        ? { __raw: `title="${options.title.replaceAll('"', "")}"` }
-        : undefined,
+      meta: options.meta
+        ? { __raw: options.meta }
+        : options.title
+          ? { __raw: `title="${options.title.replaceAll('"', "")}"` }
+          : undefined,
       themes: options.themes ?? DEFAULT_CODE_THEMES,
       transformers: [
         ...blumeShikiTransformers({ icons: options.icons }),
