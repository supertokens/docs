
```bash
curl --location --request POST '^{appInfo.apiDomain}^{appInfo.apiBasePath}/signin' \
--header 'Content-Type: application/json; charset=utf-8' \
--data-raw '{
    "formFields": [{
        "id": "email",
        "value": "john@example.com"
    }, {
        "id": "password",
        "value": "somePassword123"
    }]
}'
```
