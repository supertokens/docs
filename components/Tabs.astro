---
import type { TabsProps } from "blume/components";
import BlumeTabs from "blume/components/content/Tabs.astro";

const { hash = false, sync = false, ...props } = Astro.props as TabsProps;
---

<BlumeTabs hash={hash} sync={sync} {...props}>
  <slot />
</BlumeTabs>
