---
interface Props {
  id: string;
  level: 2 | 3 | 4 | 5 | 6;
}

const { id, level } = Astro.props;
const Heading = `h${level}` as const;
---

<Heading id={id}>
  <a class="blume-heading-anchor" href={`#${id}`}>
    <slot />
  </a>
</Heading>
